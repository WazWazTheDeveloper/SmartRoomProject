// TODO: probably need to delete this hole file

class NewDevice {
    uuid : string
    deviceType: string

    constructor(uuid: string, deviceType: string) {
        this.uuid = uuid
        this.deviceType = deviceType
    }
}